<Page x:Class="FakturacniSystem.Pages.Sklad.HistorieView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FakturacniSystem.Pages.Sklad"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HistorieView">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="7*"/>
            <ColumnDefinition Width="41*"/>
            <ColumnDefinition Width="52*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="75*"/>
            <RowDefinition Height="193*"/>
            <RowDefinition Height="182*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="1" 
                   Grid.ColumnSpan="2"
                   Margin="20,10,20,10"
                   FontSize="35"
                   Name="HistorieKoho">
            Historie
        </TextBlock>
        
        <ListView Background="AntiqueWhite" Grid.Row="1" Grid.Column="1" Name="Dodavatele">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Pocet" DisplayMemberBinding="{Binding pocetPolozekPridano}" Width="100"/>
                    <GridViewColumn Header="Dodavatel" DisplayMemberBinding="{Binding NazevDodavatele}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>

        <ListView Background="AntiqueWhite" Grid.Row="1" Grid.Column="2" Name="Odberatele" Margin="10,0,10,0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Pocet" DisplayMemberBinding="{Binding pocetPolozekOdebrano}" Width="100"/>
                    <GridViewColumn Header="Odbertatel" DisplayMemberBinding="{Binding NazevOdberatele}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Page>
