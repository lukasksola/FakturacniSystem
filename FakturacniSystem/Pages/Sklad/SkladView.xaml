<Page x:Class="FakturacniSystem.Pages.SkladView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FakturacniSystem.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SkladView">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="131*"/>
            <ColumnDefinition Width="669*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="37*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" VerticalAlignment="Bottom">
            <Button Height="50" Content="Upravit Polozku" Click="UpravitPolozku" />
            <Button Content="Pridat do skladu" Height="80" Click="PridatDoSkladu"/>
        </StackPanel>
        
        <ListView Background="AntiqueWhite" Grid.Row="2" Grid.Column="2" Name="SkladList">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Produkt" DisplayMemberBinding="{Binding Jmeno}" Width="180"/>
                    <GridViewColumn Header="Pocet" DisplayMemberBinding="{Binding Pocet}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>
        
    </Grid>
</Page>
