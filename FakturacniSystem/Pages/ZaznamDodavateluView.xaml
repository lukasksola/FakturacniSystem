<Page x:Class="FakturacniSystem.Pages.ZaznamDodavateluView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FakturacniSystem.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ZaznamDodavateluView">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="131*"/>
            <ColumnDefinition Width="669*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="37*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" VerticalAlignment="Bottom">
            <Button Height="50" Content="Upravit dodani" Click="UpravitDodavatele" />
            <Button Content="Pridat dodani" Height="80" Click="PridatDodavatele"/>
        </StackPanel>
        
        <ListView Background="LightBlue" Grid.Row="2" Grid.Column="2" Name="DodavateleList">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Produkt" DisplayMemberBinding="{Binding Item2.Jmeno}" Width="140"/>
                    <GridViewColumn Header="Pocet" DisplayMemberBinding="{Binding Item2.Pocet}" Width="200"/>
                    <GridViewColumn Header="Dodavatel" DisplayMemberBinding="{Binding Item1.NazevDodavatele}" Width="200"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
