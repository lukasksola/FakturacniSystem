<Page x:Class="FakturacniSystem.Pages.SkladView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FakturacniSystem.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SkladView">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="131*"/>
            <ColumnDefinition Width="669*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="37*"/>
        </Grid.RowDefinitions>
        
        <Button Content="Pridat do skladu" Grid.Row="1" Height="80" VerticalAlignment="Bottom" Click="PridatDoSkladu"/>
        
        <ListView Background="AntiqueWhite" Grid.Row="2" Grid.Column="2" Name="SkladList">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Produkt" DisplayMemberBinding="{Binding Jmeno}" Width="140"/>
                    <GridViewColumn Header="Pocet" DisplayMemberBinding="{Binding Pocet}" Width="200"/>
                </GridView>
            </ListView.View>
        </ListView>
        
    </Grid>
</Page>
