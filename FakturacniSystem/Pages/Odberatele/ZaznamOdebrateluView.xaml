<Page x:Class="FakturacniSystem.Pages.ZaznamOdebrateluView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FakturacniSystem.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ZaznamOdebrateluView">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="131*"/>
            <ColumnDefinition Width="669*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="37*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" VerticalAlignment="Bottom">
            <Button Content="Vystavit Fakturu" Height="30" Click="VystavitFakturu" Margin="20"/>
            <Button Height="50" Content="Upravit odber" Click="UpravitOdberatele" />
            <Button Content="Pridat odber" Height="80" Click="PridatOdberatele"/>

        </StackPanel>

        <ListView Background="LightGreen" Grid.Row="2" Grid.Column="2" Name="OdberateleList">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Produkt" DisplayMemberBinding="{Binding polozka.Jmeno}" Width="180"/>
                    <GridViewColumn Header="Pocet" DisplayMemberBinding="{Binding odebirani.pocetPolozekOdebrano}" Width="100"/>
                    <GridViewColumn Header="Odberatel" DisplayMemberBinding="{Binding odebirani.NazevOdberatele}" Width="200"/>
                    <GridViewColumn Header="Cena za kus" DisplayMemberBinding="{Binding odebirani.CenaZaKus}" Width="200"/>


                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
