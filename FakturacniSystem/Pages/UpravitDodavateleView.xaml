<Page x:Class="FakturacniSystem.Pages.UpravitDodavateleView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:FakturacniSystem.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="UpravitDodavateleView">

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="50*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60*"/>
            <RowDefinition Height="60*"/>
            <RowDefinition Height="60*"/>
            <RowDefinition Height="120*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Nazev produktu" Grid.Row="1"/>
        <TextBox Grid.Column="0" Margin="20" Text="{Binding Nazev,UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Grid.Row="1"/>

        <TextBlock Grid.Column="1" Text="ID" Grid.Row="1"/>
        <TextBox Grid.Column="1" Margin="30" Text="{Binding Id}" IsReadOnly="True" Grid.Row="1"/>

        <TextBlock Grid.Row="2"  Text="Pocet" />
        <TextBox Grid.Column="0" Grid.Row="2" Margin="25" Text="{Binding pridanyPocet, UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"/>

        <TextBlock Grid.Row="2" Grid.Column="1"  Text="Nazev dodavatele" />
        <TextBox Grid.Column="1" Grid.Row="2" Margin="25" Text="{Binding NazevDodavatele, UpdateSourceTrigger=PropertyChanged,Mode=TwoWay}"/>

        <Button Grid.Row="3" Margin="30" Content="Potvrdit" FontSize="25" Click="PotrvditUpravu"/>
        <Button Grid.Row="3" Grid.Column="1" Margin="30" Content="Zrusit" FontSize="25" Click="ZrusitUpravu" />

    </Grid>
</Page>
